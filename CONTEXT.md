# 研英批改领域上下文

本文档是项目内统一的领域词汇表。代码、题库、提示词和测试应优先使用下列含义，避免把“题目”“一次练习”“一次评分”混为同一对象。

## 核心对象

| 术语 | 英文标识 | 定义与边界 |
| --- | --- | --- |
| 真题 | `ExamQuestion` | 某一年份、题型和分值对应的固定题面。它是只读内置内容，不因用户作答而改变。 |
| 题型 | `QuestionType` | `translation`、`smallEssay` 或 `bigEssay`。题型决定输入规则、提示词和评分上限。 |
| 试卷格式 | `PaperFormat` | `oldSingleEssay` 或 `modernTranslationAndEssays` 等历史格式；用于表达旧制年份的题型差异。 |
| 翻译段落 | `TranslationSegment` | 翻译文章中的一个带序号段落；其中五个段落具有 `isTarget=true`，代表需要作答的句子。 |
| 图表/图画描述 | `imageDescription` | 当原始图像不随应用发布时，对题面可见信息的核验后文字描述；不是用户答案。 |
| 练习 | `PracticeSession` | 用户针对一条真题发起的一次答题容器，保存输入方式、图片、识别稿和创建时间。 |
| 输入方式 | `InputMode` | `manual` 或 `image`；图片输入在识别完成后仍保留原图和可编辑识别稿。 |
| 识别稿 | `recognizedText` | OCR 或视觉 LLM 返回、用户可以编辑的候选文本。只有用户确认后才进入评分。 |
| 评分版本 | `GradingRevision` | 某次评分时的答案、题面、提示词、提供商、模型和结果快照。重新评分只创建新版本，不覆盖旧版本。 |

## AI 与提示词

| 术语 | 英文标识 | 定义与边界 |
| --- | --- | --- |
| 提供商配置 | `ProviderProfile` | 一个可调用的 AI/OCR 服务配置，含协议、HTTPS Base URL、凭据、请求头、模型和视觉能力。 |
| 阶段 | `AiStage` | 识别、严格评分或复习指导中的一个可重试工作单元。翻译、小作文、大作文各自有评分和指导阶段。 |
| 阶段绑定 | `StageAssignment` | 将某个阶段绑定到提供商和模型；练习级覆盖只影响当前练习。 |
| 提示词模板 | `PromptTemplate` | 某阶段的 system prompt、user template、评分细则和修订号；提示词不是提供商配置的一部分。 |
| 评分细则 | `Rubric` | 项目自定义的结构化评分参考，不宣称为官方评分规则。 |
| 原始响应 | `rawResponse` | 服务商返回的未经改写的文本或 JSON。结构化解析失败时必须保留并展示，以便用户判断。 |
| BYOK | `Bring Your Own Key` | 用户自行填写并承担费用的服务密钥模式；项目没有中转服务或共享密钥。 |

## 不变量

1. 真题内容、题型和满分是内置只读数据；用户数据只能通过快照引用它们。
2. 评分调用按“严格评分 → 复习指导”分阶段执行；失败、取消或超时不会隐式重试或切换提供商。
3. OCR/视觉识别结果在用户确认前只能作为识别稿，不能直接成为评分答案。
4. 提示词与提供商配置独立持久化；修改任一配置不会篡改历史版本中的快照。
5. AI 返回优先解析为固定 JSON；解析失败时保存 `rawResponse`，不额外发起“修复 JSON”调用。
6. 删除历史记录必须是用户确认后的永久删除；删除失败不能静默报告成功。
7. 导出备份包含密钥时必须显示明文泄露警告，恢复必须先校验清单和哈希。

## 生命周期

```text
ExamQuestion
    ↓ 选择
PracticeSession
    ↓ 手动输入 / 图片识别稿（可编辑、待确认）
confirmed answer
    ↓ 严格评分
GradingRevision
    ↓ 复习指导（可取消、可单独重试）
历史版本与反馈
```

“提示词”“AI 提供商”是用户界面的两个独立概念；“评分版本”是历史保存单位，而不是一次网络请求的临时日志。

